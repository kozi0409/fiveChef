<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <!-- 나는 맵퍼다 선언 -->
 <mapper namespace="RecipeMapper">
 
 
 	<insert id="insertRecipe">
		INSERT INTO RECIPE_TBL VALUES(RECIPE_NO_SEQ.NEXTVAL,#{recipeTitle},#{recipeIntro},'rThumbnail',#{typeCategory},#{wayCategory},'comPhoto1','comPhoto2','comPhoto3','comPhoto4',0,0,#{userId},#{recipeLevel},#{recipeTime},#{recipePerson},DEFAULT)
 	</insert>
 	<insert id="insertIngradient">
 		INSERT INTO INGRADIENT_TBL VALUES(ING_NUM_SEQ.NEXTVAL,RECIPE_NO_SEQ.CURRVAL,#{ingBundleName},#{ingAmount},#{largeCatId},#{smallCatId})
 	</insert>
 	<insert id="insertOrder">
 		INSERT INTO ORDER_TBL VALUES(ORDER_NUM_SEQ.NEXTVAL,RECIPE_NO_SEQ.CURRVAL,#{recipeContents},#{recipePhoto})
 	</insert>
 
 
 
 </mapper>